# Copier ce fichier en .env.local et remplir les valeurs
# Les variables EXPO_PUBLIC_* sont accessibles dans le code (bundlées côté client)

EXPO_PUBLIC_API_URL=http://localhost:5158

# Actualités — agenda public de la Métropole de Lyon (OpenAgenda).
# Clé publique en lecture seule : elle est faite pour être embarquée dans une
# application cliente, et n'ouvre aucun droit d'écriture.
